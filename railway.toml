# Railway configuration for monorepo
# This file configures Railway to deploy the backend service from the backend directory

[build]
builder = "nixpacks"
buildCommand = "cd backend && npm ci && npm run build"

[deploy]
startCommand = "cd backend && npm start"

[variables]
NIXPACKS_NO_DEFAULT_CACHE_DIRS = "true"
